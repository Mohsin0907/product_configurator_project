# Telegram bot configuration
BOT_TOKEN=123456:ABCDEF-your-telegram-bot-token

# Gateway base URL (FastAPI) that the bot will call
# Example for local dev if gateway runs on port 8000
GATEWAY_URL=http://127.0.0.1:8000

# Filter new template search by creator in /config/start
CREATED_BY=your.user@example.com

# Optional: force a specific stock location in future (currently unused)
QTY_LOCATION_ID=

